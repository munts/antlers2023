<flynt-component name="NavigationFooter">
  <div class="container">
    <div class="gmc">
    Proudly Sponsored by<br>
      <img src="{{gmcLogo.src}}" style="max-height:80px;">
    </div>
    {% if contentHtml %}
      <div class="content">{{ contentHtml|e('wp_kses_post') }}</div>
    {% endif %}
  </div>
</flynt-component>
